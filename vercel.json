{
  "outputDirectory": "dist/sakan-app",
  "buildCommand": "rm -f package-lock.json && npm cache clean --force && npm install --legacy-peer-deps --force && npm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}